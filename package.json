{
  "name": "stockpulse-proxy",
  "version": "1.0.0",
  "description": "StockPulse news proxy — no API keys needed",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
